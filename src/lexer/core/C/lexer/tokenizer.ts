// 记录位置
class Position {
  line: number
  column: number
  index: number
  constructor(line: number, column: number, index: number) {
    this.line = line
    this.column = column
    this.index = index
  }
}

export class Tokenizer {}
